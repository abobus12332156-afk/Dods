background {
        monitor = 
        path = ~/Documents/Wallpapers/TLoUII/20260725102717_1.jpg
        blur_passes = 4
        blur_size = 10
        noise = 0.015
        contrast = 0.90
        brightness = 0.78
        vibrancy = 0.12
        vibrancy_darkness = 0.0
}

label {
    monitor =
    text = cmd[update:1000] echo "$(date +"%H:%M")"
    color = rgba(255, 255, 255, 0.95)
    font_size = 96
    font_family = JetBrains Mono

    position = 0, 170
    halign = center
    valign = center
}

label {
    monitor = 
    text = cmd[update:60000] echo "$(date +'%A, %d %B')"
    color = rgba(255, 255, 255, 0.55)
    font_size = 22
    font_family = JetBrains Mono
    position = 0, 75
    halign = center
    valign = center
}

shape {
    monitor =

    size = 420, 250

    color = rgba(12, 15, 20, 0.58)

    rounding = 20

    border_size = 1
    border_color = rgba(255, 255, 255, 0.10)

    position = 0, -120
    halign = center
    valign = center
}

label {
    monitor =
    text = Nikita
    color = rgba(255, 255, 255, 0.90)
    font_size = 24
    font_family = JetBrains Mono

    position = 0, -70
    halign = center
    valign = center
}

input-field {
    monitor =

    size = 330, 55

    outline_thickness = 1

    inner_color = rgba(255, 255, 255, 0.055)
    outer_color = rgba(255, 255, 255, 0.10)

    font_color = rgba(255, 255, 255, 0.92)

    fade_on_empty = false
    placeholder_text = Password...

    rounding = 13

    position = 0, -125
    halign = center
    valign = center
}
